{
  addr: {
    server: {// 服务器信息，也是浏览器访问终点。
      port: 18000,
      addr: 'localhost',
      prefix: 'http://'
    },
    ssr_prefix: 'http://localhost:18000'// 服务端渲染的请求目标地址
  },
  // admin secret
  secret: 'abcdefg',
  // 只允许管理员发送新的留言板消息。
  admin_message_only: true,
  max_length: {
    comment: 400,
    username: 10
  },
  page_items: {
    // 每页显示的对象数量
    blog_list: 5,
    bbs: 5
  }
}
